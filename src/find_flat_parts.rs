use crate::*;

/*
pub struct FlatPartFinder {
    flat_parts: Vec<usize>,
}
impl FlatPartFinder {
    /// Returns triangle indices aligned with the normal
    pub fn find_parts_aligned_with_normal(&mut self, normal: Vec3, mesh: &MeshData) -> Vec<usize> {
        /*
        for (i, [i0, i1, i2]) in mesh.indices.iter().enumerate() {
            let v0 = mesh.positions[i0 as usize];
            let v1 = mesh.positions[i1 as usize];
            let v2 = mesh.positions[i2 as usize];

            // First check that the tri faces towards the ray.
            let normal = (v1 - v0).cross(v2 - v1);
            if normal.normalized().dot(normal) > 0.8 {
                flat_parts.push(i)
                let center = (v0 + v2 + v3) / 3.0;
            }
        }
        */
    }
}
*/
